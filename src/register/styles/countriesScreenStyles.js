import {StyleSheet} from 'react-native';
import {metrics, colors} from '../../themes';

const countyAddressScreenStyles = StyleSheet.create({
  container: {
    backgroundColor: colors.white,
    flex: 1,
  },
  searchBarContainer: {
    paddingVertical: metrics.size30,
  },
});
export default countyAddressScreenStyles;
